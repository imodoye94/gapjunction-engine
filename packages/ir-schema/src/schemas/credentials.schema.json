{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://gapjunction.dev/schemas/credentials.schema.json",
  "title": "GapJunction CredentialsRef",
  "description": "Opaque reference to a secret stored in a provider.",
  "type": "object",
  "properties": {
    "type": { "const": "secretRef" },
    "ref": { "type": "string", "minLength": 1 }
  },
  "required": ["type", "ref"],
  "additionalProperties": false
}
